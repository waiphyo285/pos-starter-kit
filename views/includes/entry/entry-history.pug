include ../other/breadcrumb
  block items
    if page.entry.breadcrumb.length > 0
      each breadcrumbObj, index in page.entry.breadcrumb
        - var cl = page.list.breadcrumb.length == index ? 'font-weight-bold' : ''
        li.breadcrumb-item.d-none.d-md-inline
          a(href=breadcrumbObj.url style='color: var(--primary)' class=cl) 
            | !{breadcrumbObj.text}